version: 2.1

orbs:
  node: circleci/node@4.7

jobs:
  build:
    docker:
      - image: node:latest
    working_directory: /module5/circleci  # Update this path to match your project structure
    steps:
      - checkout
      - run:
          name: Build
          command: |
            # Add your build commands here
            echo "Building RS Software"

  deploy:
    docker:
      - image: node:latest
    working_directory: /module5/circleci  # Update this path to match your project structure
    steps:
      - checkout
      - run:
          name: Deploy
          command: |
            # Add your deployment commands here
            echo "Deploying RS Software"
            mkdir -p DEPLOY-$(date +"%Y%m%d")

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build:
          filters:
            branches:
              only:
                - main
      - deploy:
          requires:
            - build
          filters:
            branches:
              only:
                - main

